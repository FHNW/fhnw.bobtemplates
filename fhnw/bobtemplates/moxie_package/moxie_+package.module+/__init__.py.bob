from flask import Blueprint

from .views import {{{package.viewclass}}}


def create_blueprint(blueprint_name, conf):
    blueprint = Blueprint(blueprint_name, __name__, **conf)
    blueprint.add_url_rule('/', view_func={{{package.viewclass}}}.as_view('{{{package.module}}}'))
    return blueprint
